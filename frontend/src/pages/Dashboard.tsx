/**
 * Legacy Dashboard page — redirects to main Dashboard component.
 * (Original analytics dashboard removed for open-source skeleton)
 */
import { Dashboard } from '../components/Dashboard';

export default function DashboardPage() {
  return <Dashboard />;
}
